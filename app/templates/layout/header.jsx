'use strict';

const React = require('react');

const Header = React.createClass({

    render: function () {

        return (
            <header>
            </header>
        );
    }
});


module.exports = Header;